fastapi==0.110.0
uvicorn[standard]==0.28.0
python-multipart==0.0.32

# LangGraph + LangChain (agents)
langgraph==1.2.6
langchain-core==1.4.8
langchain-openai==1.3.2
langchain-groq==1.1.3

# OpenAI SDK v2 (chỉ dùng cho embeddings — xem app/services/embeddings.py)
openai==2.43.0

# Vector DB
qdrant-client==1.8.0

# TF-IDF cosine similarity (agent2_matching keyword/tfidf scoring nodes)
# 1.5.2 không có wheel cho Python 3.14 (không có compiler để build từ source) — bump lên bản có sẵn wheel
scikit-learn==1.9.0

# Pydantic v2
pydantic==2.13.4
pydantic-settings==2.14.1

# HTTP client (callback tới NestJS BE)
httpx==0.28.1

# Env
python-dotenv==1.0.1
