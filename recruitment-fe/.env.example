# ============================================================
# recruitment-fe — Environment Variables (Vite)
# Copy file này thành .env và điền giá trị thực
# Biến Vite phải có prefix VITE_ mới được expose ra browser
# KHÔNG commit file .env vào git
# ============================================================

VITE_API_BASE_URL=http://localhost:3000/api
VITE_APP_NAME="Smart Recruitment"
